@model IEnumerable<MiniApp.Models.MainCategoryTbl>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.MainCategory)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.MainCategory)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.MainCategoryID }) |
            @Html.ActionLink("Details", "Details", new { id=item.MainCategoryID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.MainCategoryID })
        </td>
    </tr>
}

</table>
