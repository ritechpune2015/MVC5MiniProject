@model IEnumerable<MiniApp.Models.DispatchAgencyTbl>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.DispatchAgencyName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Address)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.EmailID)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.DispatchAgencyName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Address)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.EmailID)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.DispatchAgencyID }) |
            @Html.ActionLink("Details", "Details", new { id=item.DispatchAgencyID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.DispatchAgencyID })
        </td>
    </tr>
}

</table>
